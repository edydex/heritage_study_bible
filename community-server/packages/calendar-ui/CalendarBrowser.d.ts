import type { ComponentType, ReactNode } from 'react'
declare const CalendarBrowser: ComponentType<{ load?: (path: string) => Promise<any>, renderActions?: (event: any) => ReactNode }>
export default CalendarBrowser
